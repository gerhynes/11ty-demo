<!doctype html>
<html lang="en">
<body>
  <header>
    <a href="/">My 11ty site</a>
  </header>
  <main>
      {% if title %} <h1>{{ title }}</h1>{% endif %}
      {%- if dog %} 
      <img src="{{dog}}" at="A good doggo"/>
      {%- endif %}
      
      {{ content }}
  </main>
</body>
</html>